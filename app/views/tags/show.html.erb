<h1> Article Tagged with <%= @tag.name %> </h1>
<ul>
	<% @tag.articles.each do |article| %>
		<li><%= link_to article.title, article_path(article) %></li>
		<li><%= link_to 'delete', tags_path, method: :delete, confirm: 'Are you sure you want to do that?' %></li>
	<% end %>

</ul>
